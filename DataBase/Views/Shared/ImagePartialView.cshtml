@model string[]

@foreach (string x in Model)
{
    <div class="float-left m-2  disable-select">
        <div class="mb-2">
            <img src="@Url.Action("GetImage","Job",new { Imagename=@x})" style="max-height:300px; max-width:300px" />
        </div>
    </div>
}